# Bun
node_modules/

# Entorn i secrets
.env
*.pem
*.key

# El full d'estil es compila; el codi font es a src/styles/app.css.
public/app.css

# Sortida de `drizzle-kit pull`, nomes per comparar contra la base de dades viva.
.introspect/
dk.config.ts

# Copies de seguretat locals
deploy/backups/*.sql.gz
